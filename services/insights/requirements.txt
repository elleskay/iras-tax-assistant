# Operational-insights pipeline dependencies.
#
# The pipeline runs with NO optional deps via a pure-numpy fallback
# (hashing TF-IDF + numpy KMeans). numpy + pytest are the only hard requirements.
#
# Installing the optional packages upgrades the embedding/cluster quality:
#   - scikit-learn         -> TF-IDF vectoriser + KMeans
#   - sentence-transformers-> all-MiniLM-L6-v2 semantic embeddings (best)
#
# The pipeline auto-detects what is installed and reports which path ran
# (see the "_meta.embeddingPath" / "_meta.clusterPath" fields in the output).

# --- hard requirements ---
numpy>=1.24
pytest>=7.0

# --- optional, better quality (uncomment / pip install if available) ---
# scikit-learn>=1.3
# sentence-transformers>=2.2
